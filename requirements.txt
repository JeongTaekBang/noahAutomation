# Core dependencies
pandas>=1.5.0,<3.0.0
openpyxl>=3.0.0,<4.0.0
xlwings>=0.30.0

# Outlook 메일 발송 (create_ts.py --mail/--send) — Windows 전용
pywin32>=305; sys_platform == "win32"

# Dashboard
streamlit>=1.35.0
plotly>=5.18.0

# Development dependencies
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<6.0.0
